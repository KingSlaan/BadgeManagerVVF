export interface VersionInfo {
  version: string;
  environment: string;
  buildDate: string;
}
export interface VersionInfoBE {
  version: string;
  appName: string;
  codeName: string;
  buildDate: string;
}
